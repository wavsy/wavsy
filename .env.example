# Server only. Never prefix with NEXT_PUBLIC_. Used to open WhatsApp after form submit.
WHATSAPP_E164=
NEXT_PUBLIC_ASSISTANT_ENABLED=false
NEXT_PUBLIC_ANALYTICS_ENABLED=false
# Umami Cloud website id (Settings → Websites). Analytics stays off without it.
NEXT_PUBLIC_UMAMI_WEBSITE_ID=
